{
  "name": "zanzibar-trail-tours",
  "version": "1.0.0",
  "description": "Zanzibar Trail Tours - Flutter Web App",
  "scripts": {
    "build": "chmod +x build.sh && bash build.sh",
    "vercel-build": "chmod +x build-isolated.sh && bash build-isolated.sh"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "-": "^0.0.1"
  }
}